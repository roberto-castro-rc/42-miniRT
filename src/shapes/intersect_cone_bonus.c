#include "minirt_bonus.h"

/*
** intersect_cone - stub for Step 3 (cone bonus)
** Will implement ray-cone intersection
*/
t_hit	intersect_cone(t_ray ray, t_cone cone, double closest)
{
	(void)ray;
	(void)cone;
	(void)closest;
	return (create_no_hit());
}
